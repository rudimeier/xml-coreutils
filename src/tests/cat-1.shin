#!/bin/sh
# check that xml-cat doesn't modify input
PATH=/bin:/usr/bin
XMLCAT=$TESTBIN/xml-cat
TMP_PATH="`pwd`/`basename $0 .sh`_`date +"%Y%m%dT%H%M%S"`"

mkdir "$TMP_PATH"

$XMLCAT cat-1.verify > "$TMP_PATH/output"

diff "$TMP_PATH/output" cat-1.verify

RESULT=$?
rm -rf "$TMP_PATH"
exit $RESULT
